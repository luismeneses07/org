import "./Header.css"
const Header = () => {
    return (
        <header className="header">
            <img src='/img/heder.png' alt='heder' className="img"/>
        </header>
    );
};

export default Header;